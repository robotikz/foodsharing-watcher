# Example environment file
# Copy this file to .env.local, .env.emulator, or .env.firebase and fill in values

# Frontend proxy URL (used by Vite)
# For local: http://localhost:8787/proxy
# For emulator: http://localhost:5001/foodsharing-watcher/europe-west3/proxy
# For firebase: /proxy (handled by Firebase hosting rewrites)
VITE_PROXY_URL=http://localhost:8787/proxy

# Backend proxy server environment variables (for server/proxy.mjs)
# These are used when running the proxy server locally or in emulator
PORT=8787
FOODWATCH_LOGIN_EMAIL=your-email@example.com
FOODWATCH_LOGIN_PASSWORD=your-password

# SMTP configuration for email notifications
FOODWATCH_SMTP_HOST=smtp.example.com
FOODWATCH_SMTP_PORT=587
FOODWATCH_SMTP_USER=your-smtp-user
FOODWATCH_SMTP_PASS=your-smtp-password
FOODWATCH_NOTIFY_FROM=noreply@example.com
FOODWATCH_NOTIFY_TO=notify@example.com
